#include<stdio.h>
#include<string.h>
int main(){
	char str1[50];
	printf ("nhap vao chuoi:\n");
	gets(str1);
	puts(str1);
    int len = strlen(str1);
    printf ("do dai chuoi co trong khoan la %d",len);
    for (int i=0; i< len; i++){
    	
	}
	if ()
	}
